# GLPI Docker Environment Configuration
# Copy this file to .env and adjust values as needed

# Application Port
PORT=80

# Timezone
TIMEZONE=Africa/Dar_es_Salaam

# Database Configuration
# DB_HOST: Use 'db' for local container, or your external database host
DB_HOST=db
DB_PORT=3306
DB_DATABASE=glpi
DB_USER=glpi
DB_PASSWORD=glpi_password_change_me

# Only needed if using local database container
DB_ROOT_PASSWORD=glpi_root_password_change_me

# For Dokploy deployment:
# - To use LOCAL database: Keep DB_HOST=db
# - To use EXTERNAL database: Set DB_HOST to your database server IP/hostname
#   Example: DB_HOST=mysql.yourserver.com or DB_HOST=192.168.1.100
